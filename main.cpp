#include <iostream>
#include <array>
#include <algorithm>
#include <string.h>

#include "Primitives.h"
#include "Plane.h"
#include "Utilities.h"
#include "StageObjects.h"

using namespace Geometry;
using namespace std;

int main () {

    /* Box box(Point(0., 0., 0.), Point(3., 2., 5.));

    for (const auto& surface : box.getSurfaces()) {
        cout << surface << endl;
    } */

    char* chars = new char[8];
    cout << strlen(chars) << endl;

    return 0;
}

